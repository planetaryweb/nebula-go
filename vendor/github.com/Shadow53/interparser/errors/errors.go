package errors

const ErrNilInterface = "nil interface{}"

const ErrNotInt64 = "could not parse as integer"
const ErrNotFloat64 = "could not parse as float"
const ErrNotBool = "could not parse as bool"
const ErrNotString = "could not parse as string"

const ErrNotSlice = "could not parse as slice"
const ErrNotMapStringKeys = "could not parse a map with string keys"
