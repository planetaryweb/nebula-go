package config

// DefaultConfigFile contains the default location of the server's
// configuration file (on FreeBSD systems)
const DefaultConfigFile = "/usr/local/etc/nebula-forms/config.toml"

// DefaultErrorFile contains the default location of the server's
// error log file (on FreeBSD systems)
const DefaultErrorFile = "/var/log/nebula-forms/error.log"

// DefaultLogFile contains the default location of the server's
// log file (on FreeBSD systems)
const DefaultLogFile = "/var/log/nebula-forms/access.log"

// DefaultDebugFile contains the default location of the server's
// debug file (on FreeBSD systems)
const DefaultDebugFile = "/var/log/nebula-forms/debug.log"

// DefaultPluginDir contains the default location of handler plugins
// (on FreeBSD systems)
const DefaultPluginDir = "/usr/local/lib/nebula-forms/plugins"